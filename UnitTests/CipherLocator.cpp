#include "stdafx.h"
#include "CppUnitTest.h"
#include "../Base/CipherLocator.h"
#include "../Abstract/Cipher.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;
using namespace std;


namespace UnitTests
{
	/*
	TEST_CLASS(CipherLocatorTest)
	{
	public:

		TEST_METHOD(Constructor)
		{
			CipherLocator *t = new CipherLocator();
		}

		TEST_METHOD(GetCipher)
		{
			CipherLocator *t = new CipherLocator();
			Key *testKey1 = new Key("WhiteRemove", "");
			Key *testKey2 = new Key("GaderyPoluki", "");

			AbstractCipher *whr = t->getCipher(testKey1);
			AbstractCipher *gp = t->getCipher(testKey2);

			Assert::AreEqual((string)"WhiteRemove", whr->getName());
			Assert::AreEqual((string)"GaderyPoluki", gp->getName());
		}


	};
	*/
}